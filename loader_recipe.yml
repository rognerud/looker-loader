recipes:
  - name: group_ids
    actions:
      group_label: Identifiers
    filters:
      regex_include: '_id$'
  - name: second_values
    filters:
      regex_include: '_seconds$'
      type: int
    dimension:
      group_label: Duration
      variants:
        - prefix: "minutes_"
          sql: "$x/60"